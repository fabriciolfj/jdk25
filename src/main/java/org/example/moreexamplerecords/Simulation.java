package org.example.moreexamplerecords;

public class Simulation {

    static void main() {
        final var point = new Point(1, "test");

        if (point instanceof Point(int x, String y)) {
            IO.println(x + "-" + y);
        }
    }
}
